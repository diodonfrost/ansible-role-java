---
- name: Verify
  hosts: all
  tasks:
    - name: FreeBSD | Java should be functionnal
      ansible.builtin.command: java -version
      register: java_result
      changed_when: false

    - name: FreeBSD | Verify commands outputs
      ansible.builtin.assert:
        that:
          - java_result.rc == 0
        success_msg: "All tests passed"
